import streamlit as st

# Setup Halaman
st.set_page_config(page_title="Christian Winanto | Portofolio", page_icon="⚽")

# --- CSS CUSTOM (Agar lebih keren) ---
st.markdown("""
    <style>
    .main { background-color: #f0f2f6; }
    stButton>button { width: 100%; border-radius: 20px; }
    </style>
    """, unsafe_allow_html=True)

# --- SIDEBAR ---
st.sidebar.title("Kontak")
st.sidebar.write("📍 Jakarta, Indonesia")
st.sidebar.write("📧 [Email Christian](mailto:christian@example.com)")
st.sidebar.info("Siswa SMA Prestasi Prima")

# --- HERO SECTION ---
col1, col2 = st.columns([1, 2], gap="large")
with col1:
    # Ganti 'profile.png' dengan link foto kamu nanti
    st.image("https://via.placeholder.com/300", caption="Christian Winanto")

with col2:
    st.title("Christian Winanto")
    st.write("📅 **Lahir:** 31 Desember 2009")
    st.write("🏫 **Sekolah:** SMA Prestasi Prima")
    st.write("🎯 **Fokus:** Student Athlete & Tech Enthusiast")
    st.write("---")
    st.write("""
        Halo! Saya Christian. Saya seorang siswa yang sangat menyukai dunia olahraga, 
        terutama **Futsal**. Bagi saya, futsal bukan sekadar hobi, tapi tempat saya 
        belajar tentang kerja keras, kerja tim, dan strategi.
    """)

# --- KEUNGGULAN (FUTSAL) ---
st.header("⚽ Keunggulan di Lapangan")
kiri, kanan = st.columns(2)
with kiri:
    st.success("**Stamina & Speed**")
    st.info("**Visi Bermain (Playmaker)**")
with kanan:
    st.warning("**Akurasi Shooting**")
    st.error("**Disiplin Bertahan**")

# --- PROGRESS SKILL ---
st.subheader("Skill Stats")
st.write("Dribbling")
st.progress(85)
st.write("Passing")
st.progress(90)
st.write("Physical")
st.progress(80)

# --- FOOTER ---
st.write("---")
st.caption("Dibuat dengan Python & Streamlit | 2024 Christian Winanto")
